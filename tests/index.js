import test from 'tape';
import pitchFromScaleIndex from '../client/scripts/tools/pitchFromScaleIndex';

test('pitchFromScaleIndex', t => {
  const major = [0, 2, 4, 5, 7, 9, 11, 12];
  t.equal(1, 1);
  t.end();
});
